import { IDemoNgComponentEventType } from './idemo-ng-component-event-type';

describe('IDemoNgComponentEventType', () => {
  it('should create an instance', () => {
    expect(new IDemoNgComponentEventType()).toBeTruthy();
  });
});
